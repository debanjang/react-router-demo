{
  "name": "topix",
  "version": "0.1.0",
  "private": true,
  "homepage": "https://debanjang.github.io/react-router-demo",
  "dependencies": {
    "gh-pages": "^2.2.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router-dom": "5.0.0"
  },
  "devDependencies": {
    "react-scripts": "3.0.1"
  },
  "resolutions": {
    "babel-jest": "24.8.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "predeploy": "yarn build",
    "deploy": "gh-pages -d build"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
